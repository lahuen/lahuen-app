// Placeholder — implemented in Fase 4
export function renderStockMovimientos(container: HTMLElement): (() => void) | null {
  container.innerHTML = `<div class="page"><div class="empty-state"><p>Movimientos — en desarrollo</p></div></div>`;
  return null;
}
