// Placeholder — implemented in Fase 2
export function renderCrmForm(container: HTMLElement, _prospectoId?: string): (() => void) | null {
  container.innerHTML = `<div class="page-narrow"><div class="empty-state"><p>Formulario prospecto — en desarrollo</p></div></div>`;
  return null;
}
