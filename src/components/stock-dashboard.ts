// Placeholder — implemented in Fase 4
export function renderStockDashboard(container: HTMLElement): (() => void) | null {
  container.innerHTML = `<div class="page"><div class="empty-state"><p>Stock — en desarrollo</p></div></div>`;
  return null;
}
