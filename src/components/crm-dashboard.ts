// Placeholder — implemented in Fase 2
export function renderCrmDashboard(container: HTMLElement): (() => void) | null {
  container.innerHTML = `<div class="page"><div class="empty-state"><p>CRM Dashboard — en desarrollo</p></div></div>`;
  return null;
}
